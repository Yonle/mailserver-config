- hosts: list of ips used to send mail to outside
- mailname: basically a file containing a line of your email hostname
- postgresql.conf: configuration to connect to your postgresql backend

NOTE: for postgresql schema, look on `pgsql_schemas` on the root dir!
